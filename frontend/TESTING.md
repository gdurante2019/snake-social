# Frontend Tests Walkthrough

## How to Run Tests

To run the frontend tests, navigate to the `frontend` directory and run the following command:

```bash
cd frontend
npm run test:run
```

This uses `vitest` to execute the tests.

## Test Results

Ran 2 test files:
- `src/__tests__/api.test.ts`
- `src/__tests__/gameLogic.test.ts`

**Total Tests**: 44
**Passed**: 44
**Failed**: 0

## Configuration

The tests are configured in `frontend/vitest.config.ts`.
